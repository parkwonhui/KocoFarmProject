<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.oeg.dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kocofarm.mapper.module.EmpMapper">

	<!-- 목록 -->
	<select id="getEmpList" resultType="org.kocofarm.domain.emp.EmpVO">
		SELECT
		<!-- <include refid="basicCol" /> -->
		FROM EMP A
		LEFT JOIN DEPARTMENTS B ON A.DEPT_ID = B.DEPT_ID
		LEFT JOIN
		JOBS C ON A.JOB_ID = C.JOB_ID
		LEFT JOIN POSITION D ON A.POSITION_ID =
		D.POSITION_ID
		<!-- <if test="null != schType and '' != schType and null != schWord and 
			'' != schWord"> <where> <if test="'empId' == schType"> A.EMP_ID = #{schWord} 
			</if> <if test="'korNm' == schType"> A.KOR_NM LIKE '%'||#{schWord}||'%' </if> 
			<if test="'cellphone' == schType"> A.CELLPHONE = #{schWord} </if> <if test="'state' 
			== schType"> A.STATE = #{schWord} </if> </where> </if> -->
		ORDER BY A.REG_DT DESC
	</select>

	<!-- 상세정보 -->
	<select id="getEmp" resultType="org.kocofarm.domain.emp.EmpVO">
		SELECT
		<!-- <include refid="basicCol" /> -->
		FROM EMP A
		LEFT JOIN DEPARTMENTS B ON A.DEPT_ID = B.DEPT_ID
		LEFT JOIN
		JOBS C ON A.JOB_ID = C.JOB_ID
		LEFT JOIN POSITION D ON A.POSITION_ID =
		D.POSITION_ID
		WHERE A.EMP_ID =
		#{empId}
	</select>


	<!-- 부서 목록 -->
	<select id="getDeptList" resultType="org.kocofarm.domain.emp.DeptVO">
		SELECT
		DEPT_ID AS deptId
		,
		DEPT_NM AS deptNm
		, MANAGER_ID AS managerId
		FROM DEPARTMENTS
	</select>


</mapper>